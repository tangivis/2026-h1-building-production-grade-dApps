{
  "name": "storage-contract",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat ignition deploy ./ignition/modules/Storage.ts --network polkadotTestNet",
    "clean": "hardhat clean && rm -rf ignition/deployments",
    "recompile": "hardhat clean && rm -rf ignition/deployments && hardhat compile",
    "redeploy": "hardhat clean && rm -rf ignition/deployments && hardhat compile && hardhat ignition deploy ./ignition/modules/Storage.ts --network polkadotTestNet",
    "send-op-tx": "npx ts-node scripts/send-op-tx.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@nomicfoundation/hardhat-ignition": "^3.0.7",
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.2",
    "@types/node": "^22.19.10",
    "forge-std": "github:foundry-rs/forge-std#v1.9.4",
    "hardhat": "^3.1.7",
    "typescript": "~5.8.0",
    "viem": "^2.45.1"
  }
}